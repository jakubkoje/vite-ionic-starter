import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'dev.wezeo.viteionicstarter',
  appName: 'vite-ionic-starter',
  webDir: 'dist',
  bundledWebRuntime: false
}

export default config
